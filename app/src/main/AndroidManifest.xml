<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ivan.electric">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Электромеханика, основы"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ElectricsActivity"
            android:label="Электрика"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PneumaticActivity"
            android:label="Пневматика"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MehanicActivity"
            android:label="Механика"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>